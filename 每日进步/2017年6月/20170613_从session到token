session的最大用途是在服务器中区别用户身份，其次保存用户的操作行为和临时数据。每次用户请求都携带session信息，服务器判断session是否存在，然后知道用户是否已经登录。

token的作用同session类似，服务器将token颁发给用户，用户每次都将token发送给服务器，用于验证用户身份。不同的是，颁发时token和用户ID用密钥加密，收到请求时再用密钥解密，这样就能验证用户身份了。

Token就是令牌
